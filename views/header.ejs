<header class="bg-green-300 shadow-md px-6 py-4">
  <div class="max-w-6xl mx-auto flex justify-between items-center">
    <a href="/" class="text-2xl font-bold text-green-700">🌾 AgriFusion</a>

    <nav class="flex gap-4 items-center">
      <a href="/" class="text-gray-700 hover:text-green-700">Home</a>
      <a href="/products" class="text-gray-700 hover:text-green-700">Products</a>
      <a href="/about" class="text-gray-700 hover:text-green-700">About</a>
      <a href="/nearby-farmers" class="text-gray-700 hover:text-green-700">Nearby Farmers</a>

      <% if (user && user.role === 'farmer') { %>
        <a href="/messages" class="text-gray-700 hover:text-green-700 font-semibold">📨 Messages</a>
      <% } %>

      <% if (user) { %>
        <span class="text-green-800 font-semibold">👤 <%= user.name %></span>
        <form method="POST" action="/logout" class="inline">
          <button type="submit" class="text-red-600 hover:underline ml-2">Logout</button>
        </form>
      <% } else { %>
        <a href="/login" class="text-gray-700 hover:text-green-700">Login</a>
        <a href="/register" class="text-gray-700 hover:text-green-700">Sign Up</a>
      <% } %>
    </nav>
  </div>
</header>
