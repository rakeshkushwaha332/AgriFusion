<!DOCTYPE html>
<html lang="en">
<head>
  <title>Edit Product</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <%- include('header') %>

  <div class="max-w-xl mx-auto bg-white p-6 mt-10 rounded shadow">
    <h2 class="text-xl font-bold mb-4 text-green-700">Edit Product</h2>

    <form method="POST" action="/products/edit/<%= product._id %>" class="space-y-4">
      <input type="text" name="title" value="<%= product.title %>" required class="w-full border p-2 rounded" />

      <textarea name="description" required class="w-full border p-2 rounded"><%= product.description %></textarea>

      <input type="text" name="imageUrl" value="<%= product.imageUrl %>" required class="w-full border p-2 rounded" />

      <input type="number" name="currentMarketPrice" value="<%= product.currentMarketPrice %>" required class="w-full border p-2 rounded" />

      <input type="number" name="sellingPrice" value="<%= product.sellingPrice %>" required class="w-full border p-2 rounded" />

      <button type="submit" class="w-full bg-green-600 text-white py-2 rounded hover:bg-green-700">Update Product</button>
    </form>
  </div>

  <%- include('footer') %>
</body>
</html>
