<!DOCTYPE html>
<html lang="en">
<head>
  <title>Register</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body >
  <%- include('header') %>

  <div class="bg-gray-100 flex justify-center items-center h-screen">

    <div class="bg-white p-8 rounded shadow-md w-full max-w-sm">
      <h2 class="text-2xl font-bold text-center mb-6">Register</h2>
      <form method="POST" action="/register" class="space-y-4">
  
        <input type="text" name="name" placeholder="Full Name"
          class="w-full px-4 py-2 border rounded" required />
  
        <input type="email" name="email" placeholder="Email"
          class="w-full px-4 py-2 border rounded" required />
  
        <input type="password" name="password" placeholder="Password"
          class="w-full px-4 py-2 border rounded" required />
  
        <select name="role" id="role" class="w-full px-4 py-2 border rounded" required onchange="toggleLocationFields(this.value)">
          <option value="">Select Role</option>
          <option value="farmer">Farmer</option>
          <option value="customer">Customer</option>
        </select>
  
        <div id="locationFields" style="display: none;">
          <input type="text" name="city" placeholder="City" class="w-full px-4 py-2 border rounded mt-2" />
          <input type="text" name="state" placeholder="State" class="w-full px-4 py-2 border rounded" />
          <input type="text" name="country" placeholder="Country" class="w-full px-4 py-2 border rounded" />
          <input type="text" name="pincode" placeholder="Pincode" class="w-full px-4 py-2 border rounded" />
        </div>
  
        <button type="submit" class="w-full bg-green-500 text-white py-2 rounded hover:bg-green-600">Register</button>
      </form>
  
      <p class="text-center mt-4 text-sm">
        Already have an account?
        <a href="/" class="text-blue-600 hover:underline">Login here</a>
      </p>
    </div>
  </div>
  
  <%- include('footer') %>

  <script>
    function toggleLocationFields(role) {
      const locationDiv = document.getElementById('locationFields');
      locationDiv.style.display = role === 'farmer' ? 'block' : 'none';
    }
  </script>
</body>
</html>
