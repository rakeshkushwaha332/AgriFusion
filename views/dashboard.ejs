<!DOCTYPE html>
<html>
<head>
  <title>Dashboard | AgriFusion</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-green-50 min-h-screen flex flex-col">
  <%- include('header') %>

  <main class="max-w-3xl mx-auto mt-10 bg-white p-8 rounded shadow flex-grow">
    <h1 class="text-3xl font-bold text-green-800 mb-2">Welcome, <%= user.name %>!</h1>
    <p class="text-lg text-gray-700 mb-4">You are logged in as a <strong><%= user.role %></strong>.</p>

    <% if (user.role === 'farmer') { %>
      <a href="/products/add" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded shadow">Add New Product</a>
    <% } else { %>
      <a href="/products" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded shadow">Browse Products</a>
    <% } %>
  </main>

  <%- include('footer') %>
</body>

</html>
