<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Inbox - AgriFusion</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">
  <%- include('header') %>

  <main class="flex-1">
    <div class="max-w-5xl mx-auto mt-12 px-6">
      <div class="bg-white rounded-xl shadow-md p-6">
        <h1 class="text-3xl font-bold text-green-700 mb-6">📥 Message Inbox</h1>

        <% if (conversations.length > 0) { %>
          <ul class="divide-y divide-gray-200">
            <% conversations.forEach(conv => { %>
              <li class="flex items-center justify-between py-4 hover:bg-green-50 px-4 rounded-lg transition-all duration-200">
                <div class="flex items-center gap-4">
                  <div class="bg-green-200 text-green-800 font-bold rounded-full w-10 h-10 flex items-center justify-center text-lg">
                    <%= conv.customer.name[0] %>
                  </div>
                  <div>
                    <p class="text-lg font-medium text-gray-800">👤 <%= conv.customer.name %></p>
                    <p class="text-sm text-gray-500">You have a chat with this customer</p>
                  </div>
                </div>
                <a href="/chat/<%= conv.customer._id %>" class="bg-green-600 text-white px-5 py-2 rounded hover:bg-green-700 transition">View Chat</a>
              </li>
            <% }) %>
          </ul>
        <% } else { %>
          <p class="text-center text-gray-500">No messages yet.</p>
        <% } %>
      </div>
    </div>
  </main>

  <%- include('footer') %>
</body>
</html>
